class AccountsOverviewPage {
  /**
   * @param {import('@playwright/test').Page} page
   */
  constructor(page) {
    this.page = page;
    // TODO: Define locators for Accounts Overview page
  }

  async goto() {
    // TODO: Navigate to accounts overview page
  }
}

module.exports = { AccountsOverviewPage };
